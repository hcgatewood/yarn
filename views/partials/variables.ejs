<script>
  var username = '<%= username %>';

  <% if (typeof storyId !== 'undefined') { %>
    var storyId = '<%= storyId %>';
  <% } %>

  <% if (typeof page_id !== 'undefined') { %>
    var page_id= "<%= page_id %>";
  <% } %>

  <% if (typeof id !== 'undefined') { %>
    var id= "<%= id %>";
  <% } %>

  <% if (typeof follow !== 'undefined') { %>
    var follow= "<%= follow %>";
  <% } %>

  <% if (typeof roomId !== 'undefined') { %>
    var storyId = '<%= storyId %>';
    var roomId = '<%= roomId %>';
    var roomInterval = '<%= roomInterval %>';
    var isUserTurn = '<%= isUserTurn %>';
    var isWriter = '<%= isWriter %>';
  <% } %>

  <% if (typeof userId !== 'undefined') { %>
    var userId = '<%= userId %>';
  <% } %>

</script>
